import { Header } from "@/components/layout/header";
const faqs = [["How do I place an order?", "Browse the catalog, add products to your cart, and continue to checkout."], ["Where is stock managed?", "Product availability is managed by the Baby Secret team in WooCommerce."], ["How can I contact the team?", "Use the contact page and the team will respond to your enquiry."]];
export default function FaqPage() { return <main className="min-h-screen bg-[#f9fcff] px-6 pb-20 pt-36 sm:px-10"><Header /><div className="mx-auto max-w-3xl"><h1 className="text-5xl font-medium">FAQs</h1><div className="mt-10 grid gap-4">{faqs.map(([q,a]) => <details key={q} className="glass-panel rounded-2xl p-6"><summary className="cursor-pointer font-semibold">{q}</summary><p className="mt-4 text-[#334f6d]">{a}</p></details>)}</div></div></main>; }
